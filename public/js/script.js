// document.addEventListener("DOMContentLoaded", function() {
//     // Get the menu element
//     var menu = document.getElementById("stickyMenu");

//     // Get the initial offset position of the menu using getBoundingClientRect()
//     var menuOffset = menu.getBoundingClientRect().top + window.scrollY;

//     // Add a scroll event listener
//     window.onscroll = function() {
//       // Check if the current scroll position is greater than or equal to the initial offset
//       if (window.pageYOffset >= menuOffset) {
//         // If yes, make the menu sticky
//         menu.style.position = "fixed";
//         menu.style.top = "0";
//         menu.style.width = "100%";
//       } else {
//         // If no, reset the position to the default
//         menu.style.position = "relative";
//       }
//     };
//   });
 